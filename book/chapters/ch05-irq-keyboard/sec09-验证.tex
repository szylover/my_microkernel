\section{验证}

\begin{tcblisting}{consolebox}
«\tps{\$}» «\tin{make iso \&\& make run}»
# 在 QEMU 窗口中按键，串口应看到字符回显
# 按 Shift+a 应显示 'A'
# 快速连续打字不应丢字符（除非超过 128 个未消费的字符）
\end{tcblisting}

% ============================================================================

