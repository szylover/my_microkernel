% ============================================================================
\chapter{网络工具}
\label{ch:net-tools}
% Stage H-9 ~ H-11
% ============================================================================

\section{本章目标}

\begin{itemize}
  \item 实现用户态 \texttt{/bin/ping}（ICMP Echo Request/Reply）
  \item 实现简易 HTTP client \texttt{/bin/wget}（TCP + HTTP/1.0 GET）
  \item 里程碑 H 集成测试：应用层 → Socket → TCP/IP → 网卡 → 网络 全路径
\end{itemize}

% ============================================================================
\section{用户态 ping}
\label{sec:nettools-ping}
% ============================================================================

% TODO:
% - 使用 SOCK_RAW + IPPROTO_ICMP（或专用 syscall）
% - 构造 ICMP Echo Request：type=8, code=0, id=PID, seq=递增, data=时间戳
% - 发送 → 等待 ICMP Echo Reply（type=0）
% - 计算 RTT = recv_time - send_time
% - 输出格式：64 bytes from 10.0.2.2: icmp_seq=1 ttl=64 time=0.5 ms
% - 统计汇总：N packets transmitted, N received, 0% loss, min/avg/max RTT
% - 命令行：ping [-c count] <host>

% ============================================================================
\section{简易 HTTP client}
\label{sec:nettools-wget}
% ============================================================================

% TODO:
% - TCP socket + connect 到 host:80
% - 发送 HTTP/1.0 GET 请求：
%     GET /path HTTP/1.0\r\n
%     Host: hostname\r\n
%     \r\n
% - 接收并解析响应（状态行 + 头部 + 空行 + body）
% - 将 body 输出到 stdout 或保存到文件
% - 命令行：wget <url> [-o output_file]
% - DNS 解析暂不实现（直接用 IP 地址）

% ============================================================================
\section{验证}
\label{sec:nettools-verify}
% ============================================================================

% TODO:
% - 测试 1：ping 10.0.2.2 -c 3 → 三次 RTT 正常显示
% - 测试 2：在 host 上启动 HTTP server → wget http://10.0.2.2:8080/test.txt → 内容正确
% - 里程碑 H 集成测试：
%     E1000 收发帧 + ARP 解析 + ICMP ping 往返 + TCP 连接+数据传输
%     + Socket fd 与 VFS 集成 + 用户态 ping/wget 全路径
% - shell 命令：net test

% ============================================================================
\section{本章小结}
% ============================================================================

用户态网络工具是整个网络栈的顶层验证。ping 证明了 ICMP 全路径通畅，
wget 证明了 TCP + HTTP 的完整闭环。
至此里程碑 H（网络）全部完成，SZY-KERNEL 的所有规划目标均已覆盖：
\begin{itemize}
  \item 自举 GCC 编译器 ✓
  \item 运行 Vim 编辑器 ✓
  \item TCP/IP 网络通信 ✓
\end{itemize}
